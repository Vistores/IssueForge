services:
  backend:
    build:
      context: ./backend
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__DefaultConnection: Data Source=/data/issueforge.db
      Cors__AllowedOrigins__0: http://localhost:4200
      SeedData__Enabled: "false"
    ports:
      - "5008:8080"
    volumes:
      - issueforge-data:/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  issueforge-data:
